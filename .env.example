# Optional: FairScale API key for real Score API (https://sales.fairscale.xyz/)
# Without this, the app uses demo mock scores.
# Copy to .env and set your key for local dev; in production set the same var in your host (e.g. Vercel/Netlify).
# VITE_FAIRSCALE_API_KEY=your_fairkey_here
